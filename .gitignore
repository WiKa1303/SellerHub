# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# iCloud-Platzhalter
*.icloud

# Konfliktdateien von iCloud (falls zwei Macs gleichzeitig speichern)
* [0-9].html
* Konflikt*.html

# Exportierte App-Daten (gehören nicht in die Versionierung)
sellerhub-data.json

# Node-Abhängigkeiten im Root (z. B. Playwright für Screenshots) — server/ hat eigene, echte package.json!
/node_modules/
/package.json
/package-lock.json
